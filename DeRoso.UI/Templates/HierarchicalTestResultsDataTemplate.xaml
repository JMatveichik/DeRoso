<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:DeRoso.UI.Templates"
                    xmlns:health="clr-namespace:DeRoso.Core.Health;assembly=DeRoso.Core"
                    xmlns:cb="clr-namespace:CalcBinding;assembly=CalcBinding">


    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="../Styles/TextBlockStyles.xaml"></ResourceDictionary>
        <ResourceDictionary Source="../Styles/BordersStyles.xaml"></ResourceDictionary>
        <ResourceDictionary Source="../Icons/Icons.xaml"></ResourceDictionary>
    </ResourceDictionary.MergedDictionaries>
   
    <HierarchicalDataTemplate x:Key="HierarchicalTestResultsDataTemplate" 
                              ItemsSource="{Binding Meassurments}">
        
        <Border Style="{StaticResource BorderUnderline}">
        
            <Grid Margin="5" MinWidth="800">
            <Grid.RowDefinitions>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
                
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Grid.Row="0" 
                       Style="{StaticResource ItemTitleStyle}"
                       Text="{Binding Test.Title}"/>

            <TextBlock Grid.Column="0"
                       Grid.Row="1"
                       Style="{StaticResource ItemDescriptionStyle}"
                       MaxWidth="300"
                       HorizontalAlignment="Left"
                       Text="{Binding Test.Description}" />
            
            <TextBlock Grid.Column="1"
                       Grid.Row="0"
                       Style="{StaticResource ItemDescriptionStyle}"
                       MaxWidth="300"
                       HorizontalAlignment="Left"
                       Text="Метод расчета" />

            <TextBlock Grid.Column="1"
                       Grid.Row="1"
                       Style="{StaticResource ItemDescriptionStyle}"
                       MaxWidth="300"
                       HorizontalAlignment="Left"
                       Text="{Binding Test.CalculationType}" />

        </Grid>
        </Border>
        
        <HierarchicalDataTemplate.ItemTemplate >

            <DataTemplate>
               
                <Grid Margin="5" MinWidth="800">

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="30"></ColumnDefinition>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                        <ColumnDefinition Width="10*"></ColumnDefinition>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                    </Grid.ColumnDefinitions>

                    <ContentPresenter 
                        Grid.Column="0"
                        Width="32" Height="32"
                        Content="{StaticResource IconChecked}" 
                        Visibility="{cb:Binding IsOptimal, FalseToVisibility=Hidden}">
                        
                    </ContentPresenter>
                    
                    <TextBlock Grid.Column="1" 
                                                       Style="{StaticResource ValueTitleStyle}"
                                                       HorizontalAlignment="Center"
                                                       FontSize="28"
                                                       Text="{Binding MeassurmentBefore, StringFormat={}{0:0.0}}"/>

                    <Grid Grid.Column="2" HorizontalAlignment="Center">

                        <Grid.RowDefinitions>
                            <RowDefinition></RowDefinition>
                            <RowDefinition></RowDefinition>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>

                            <ColumnDefinition Width="60"></ColumnDefinition>
                            <ColumnDefinition Width="60"></ColumnDefinition>
                            <ColumnDefinition Width="*"></ColumnDefinition>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Grid.Row="0" 
                                                            Style="{StaticResource ItemTitleStyle}"
                                                            Text="{Binding Drug.Address}"/>


                        <TextBlock Grid.Column="1"
                                                           Grid.Row="0"
                                                           Style="{StaticResource ItemTitleStyle}"
                                                           Text="{Binding Drug.Cell}" />

                        <TextBlock Grid.Column="2"
                                                           Grid.Row="0"
                                                           Style="{StaticResource ItemTitleStyle}"
                                                           TextAlignment="Left"
                                                           Text="{Binding Drug.Title}" />

                        <TextBlock Grid.Column="0"
                                                           Grid.Row="1"
                                                           Grid.ColumnSpan="3"
                                                           Style="{StaticResource ItemDescriptionStyle}"
                                                           Text="{Binding Drug.Description}" />

                    </Grid>

                    <TextBlock Grid.Column="3" 
                                                       HorizontalAlignment="Center"
                                                       FontSize="28"
                                                       Style="{StaticResource ValueTitleStyle}"
                                                       Text="{Binding MeassurmentAfter, StringFormat={}{0:0.0}}"/>

                </Grid>
            
            </DataTemplate>
        </HierarchicalDataTemplate.ItemTemplate>
        
    </HierarchicalDataTemplate>
   
    

    
    
    
</ResourceDictionary>